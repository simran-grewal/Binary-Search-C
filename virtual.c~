#include<stdio.h>
void binary(int arr[], int l, int r, int e)
{
     int mid = (l + r)/2;
    
    if(mid == e)
        return true;
}
int main()
{
    int arr[] = {7, 8, 2, 3, 4};
    int n = 10;

    int l = 0;
    int r = 10;

   

    binary(arr, l, r, e);

}
