#include<iostream>
using namespace std;
void swap(int &p, int &q)
{
    int temp = p;
        p    = q;
        q    = temp;
}
int main()
{
    int a = 5, b = 10;
    swap(a, b);

    //printf(" %d %d ", a, b);

    cout<<a<<" "<<b<<" ";
}
